jQuery(document).ready(function($) {
  console.log('here');

  setupCountries();
  setupIndicators();

  bindEvents();
});

var setupCountries = function() {};
var setupIndicators = function() {};

var getData = function(series) {
};

var bindEvents = function() {
};

